export default {
  plugins: {
    // Enable the Tailwind CSS PostCSS plugin.
    // In Tailwind CSS v4, this plugin handles all the processing,
    // including content scanning, utility generation, and optimization.
    '@tailwindcss/postcss': {},
  },
};